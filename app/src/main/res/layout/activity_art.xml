<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <LinearLayout
        android:id="@+id/titleBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginTop="8dp">

        <ImageButton
            android:id="@+id/backButton"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="@drawable/black_circle_background"
            android:src="@drawable/ic_arrow_back"
            android:contentDescription="Back" />

        <!-- Art Title TextView -->
        <TextView
            android:id="@+id/artTitleTextView"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:textSize="24sp"
            android:textStyle="bold"
            android:layout_weight="1"
            android:gravity="center"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="8dp" />

        <!-- Heart Icon Button without initial src -->
        <ImageButton
            android:id="@+id/favoriteButton"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Favorite" />
    </LinearLayout>

    <!-- Art Image -->
    <ImageView
        android:id="@+id/artImageView"
        android:layout_width="match_parent"
        android:layout_height="300dp"
        android:scaleType="centerCrop"
        android:layout_below="@id/titleBar"
        android:layout_marginTop="16dp" />

    <!-- Art Description -->
    <TextView
        android:id="@+id/artDescriptionTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/artImageView"
        android:layout_marginTop="4dp" />

    <TextView
        android:id="@+id/artArtistTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/artDescriptionTextView"
        android:layout_marginTop="4dp" />


    <!-- Art Price -->
    <TextView
        android:id="@+id/artPriceTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/artArtistTextView"
        android:layout_marginTop="4dp" />

    <!-- Art Likes -->
    <TextView
        android:id="@+id/artLikesTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/artPriceTextView"
        android:layout_marginTop="4dp" />
</RelativeLayout>
